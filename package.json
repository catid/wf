{
  "name": "warning-forever-static",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/index.html && cp -R static dist/static",
    "deploy": "echo \"Static bundle already built. Cloudflare Pages will upload dist/ for you.\""
  }
}
