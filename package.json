{
  "name": "warning-forever-pages",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "python3 export_static.py",
    "deploy": "echo \"Cloudflare Pages deploy handled via default uploader\"",
    "preview": "npm run build && npx wrangler pages dev dist --local"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
